const Layout1Settings = {
  leftSidebar: {
    show: true,
    mode: 'full', // full, close, compact, mobile,
    theme: 'plomo', // View all valid theme colors inside MatxTheme/themeColors.js
  },
  topbar: {
    show: true,
    fixed: true,
    theme: 'red', // View all valid theme colors inside MatxTheme/themeColors.js
  },
};

export default Layout1Settings;
